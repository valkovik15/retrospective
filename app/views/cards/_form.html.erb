<%= form_with(model: card, url: board_cards_path(card.board.slug), local: true) do |form| %>
  <h2>Add new '<%= card.kind %>' card</h2>
  <%= form.hidden_field(:kind) %>
  <%= form.text_field :body, class: 'input' %>
<% end %>
